<article className="lesson-card">
  <h2>Гілкування у Git (Branching)</h2>
  <p>
    Гілкування — це серце командної роботи в Git. Гілка дозволяє ізольовано розробляти нову функціональність, тестувати експерименти чи виправляти баги, не ризикуючи зламати стабільний код у головній гілці (<code>main</code> або <code>master</code>).
  </p>
</article>

<section className="analogy-block">
  <div className="analogy-block__title"><span className="analogy-block__icon">🌿</span> Гілка — як паралельний вимір у фільмі</div>
  <p>
    Уяви, що головна лінія часу — це гілка <code>main</code>. Коли ти створюєш гілку <code>feat/dark-mode</code>, ти відкриваєш портал у паралельний вимір, де робиш зміни. Якщо експеримент вдалий — ти об'єднуєш цей вимір з основною реальністю (Merge). Якщо невдалий — просто видаляєш гілку без жодного сліду в <code>main</code>.
  </p>
</section>

<section>
  <h2>🇬🇧 Англійський технічний глосарій (Vocabulary)</h2>
  <div className="grid grid-cols-1 sm:grid-cols-2 gap-3 my-4">
    <div className="p-3.5 rounded-xl border border-border/70 bg-card/60">
      <div className="font-mono font-bold text-primary text-sm">Branch (Гілка)</div>
      <div className="text-xs text-muted-foreground mt-1">Рухомий вказівник на конкретний комміт у графі історії змін проекту.</div>
    </div>
    <div className="p-3.5 rounded-xl border border-border/70 bg-card/60">
      <div className="font-mono font-bold text-primary text-sm">HEAD (Поточний стан)</div>
      <div className="text-xs text-muted-foreground mt-1">Спеціальний вказівник Git, який вказує на поточну активну гілку та поточний комміт.</div>
    </div>
    <div className="p-3.5 rounded-xl border border-border/70 bg-card/60">
      <div className="font-mono font-bold text-primary text-sm">Fast-Forward Merge</div>
      <div className="text-xs text-muted-foreground mt-1">Злиття без додаткового комміту, коли вказівник гілки просто пересувається вперед по прямій лінії.</div>
    </div>
    <div className="p-3.5 rounded-xl border border-border/70 bg-card/60">
      <div className="font-mono font-bold text-primary text-sm">3-Way Merge (Merge Commit)</div>
      <div className="text-xs text-muted-foreground mt-1">Злиття двох розбіжних гілок зі створенням окремого комміту злиття з двома батьками.</div>
    </div>
    <div className="p-3.5 rounded-xl border border-border/70 bg-card/60">
      <div className="font-mono font-bold text-primary text-sm">Merge Conflict</div>
      <div className="text-xs text-muted-foreground mt-1">Конфлікт злиття — стан, коли в обох гілках змінено ті самі рядки коду і Git потребує ручного вибору розробника.</div>
    </div>
    <div className="p-3.5 rounded-xl border border-border/70 bg-card/60">
      <div className="font-mono font-bold text-primary text-sm">Pull Request (PR / MR)</div>
      <div className="text-xs text-muted-foreground mt-1">Запит на злиття коду на GitHub/GitLab з перевіркою автотестами та код-рев'ю колег.</div>
    </div>
  </div>

  <h2>Секрет під капотом: чим насправді є гілка?</h2>
  <p>
    У більшості старих систем контролю версій гілка створювала повну копію всіх файлів. У Git гілка — це <strong>всього лише текстовий файл вагою 41 байт</strong>, що містить хеш останнього комміту (вказівник у <code>.git/refs/heads/</code>). Саме тому створення та перемикання гілок у Git відбувається за 1 мілісекунду!
  </p>

  <h2>Сучасні команди для роботи з гілками</h2>

```bash
# 1. Створення та перехід на нову гілку (сучасний синтаксис):
git switch -c feat/header-redesign

# 2. Перегляд списку всіх локальних гілок:
git branch

# 3. Перемикання на існуючу гілку:
git switch main

# 4. Злиття гілки feat/header-redesign у поточну гілку (main):
git merge feat/header-redesign

# 5. Безпечне видалення гілки після успішного злиття:
git branch -d feat/header-redesign
```

  <h2>Fast-Forward злиття проти 3-Way Merge</h2>

  <div className="syntax-params">
    <table className="params-table">
      <thead>
        <tr>
          <th>Тип злиття</th>
          <th>Коли виникає</th>
          <th>Результат у дереві коммітів</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><strong>Fast-Forward</strong></td>
          <td>Якщо в гілці <code>main</code> не з'явилося нових коммітів з моменту створення feature-гілки.</td>
          <td>Вказівник <code>main</code> просто пересувається вперед без створення додаткового комміту злиття.</td>
        </tr>
        <tr>
          <td><strong>3-Way Merge</strong> (Merge commit)</td>
          <td>Якщо і в <code>main</code>, і у feature-гілці паралельно з'явилися нові комміти.</td>
          <td>Git знаходить спільного предка і створює спеціальний <em>Merge Commit</em> з двома батьками.</td>
        </tr>
      </tbody>
    </table>
  </div>

  <h2>Угода про іменування гілок у команді</h2>
  <ul>
    <li><code>feat/login-modal</code> — нова функціональність</li>
    <li><code>fix/cart-discount-calc</code> — виправлення бага</li>
    <li><code>refactor/auth-context</code> — оптимізація та рефакторинг</li>
    <li><code>chore/deps-update</code> — оновлення пакетів та конфігів</li>
  </ul>
</section>

<section className="quizzes-section my-10">
  <h2>🎯 Бліц-перевірка знань: Гілкування та злиття</h2>

  <div className="space-y-6">
    <QuizCard
      question="Яка сучасна команда Git створює нову гілку та одразу перемикається на неї?"
      options="git branch -n feat/new | git switch -c feat/new | git checkout main feat/new | git merge -b feat/new"
      correctIndex="1"
      explanation="Команда `git switch -c <branch-name>` є сучасним та рекомендованим синтаксисом (починаючи з Git 2.23) для створення та одночасного перемикання на нову гілку (аналог старої `git checkout -b`)."
    />

    <QuizCard
      question="Що таке вказівник HEAD у Git?"
      options="Найперший комміт в історії репозиторію | Вказівник на поточну активну гілку або комміт, з яким ви зараз працюєте | Сервер GitHub | Папка зі збереженими файлами"
      correctIndex="1"
      explanation="HEAD — це спеціальний вказівник, який показує, де саме у графі коммітів ви зараз знаходитесь. Зазвичай він вказує на назву поточної гілки."
    />

    <QuizCard
      question="Коли під час git merge виникає Fast-Forward злиття?"
      options="Завжди при будь-якому злитті | Коли в цільовій гілці (main) не було нових коммітів з моменту створення поточної гілки | Тільки коли виникає merge conflict | Коли проект збирається через Webpack"
      correctIndex="1"
      explanation="Fast-forward можливий, якщо історія не розгалужувалася: комміти feature-гілки йдуть строго після останнього комміту main. Git просто переміщує стрілку main на кінцевий комміт."
    />
  </div>
</section>

<GitHubTaskCard
  title="Практикум: Feature Branching, Pull Request та злиття з автотестами"
  difficulty="medium"
  testRunner="GitHub Actions & Vitest"
  repoTemplate="https://github.com/frontend-camp/git-branching-starter"
  description="Створіть feature-гілку для нового компонента, реалізуйте функціонал, запустіть тести та зробіть злиття (merge) у головну гілку main."
  files={`src/utils/calculator.js: Функція розрахунку суми та знижок
test/calculator.test.js: Автотести для перевірки калькулятора`}
  criteria={`Створено нову гілку feat/discount-calculator командою git switch -c
Реалізовано функцію calculateDiscount у файлі src/utils/calculator.js
Усі локальні тести npm test проходять успішно (зелені)
Зроблено комміт feat: implement discount calculation with coupon support
Виконано перемикання на main та успішне злиття git merge feat/discount-calculator`}
  testFileCode={`// test/calculator.test.js
import { describe, it, expect } from 'vitest';
import { calculateDiscount } from '../src/utils/calculator.js';

describe('Discount Calculator Feature', () => {
  it('має повертати правильну суму зі знижкою 10%', () => {
    expect(calculateDiscount(1000, 10)).toBe(900);
  });

  it('повинен підтримувати промокод SAVE20 зі знижкою 20%', () => {
    expect(calculateDiscount(500, 0, 'SAVE20')).toBe(400);
  });

  it('не повинен дозволяти відʼємну ціну', () => {
    expect(() => calculateDiscount(-100, 10)).toThrow();
  });
});`}
  testFileName="calculator.test.js"
  tips={`Перед злиттям гілок завжди повертайтеся на цільову гілку (git switch main), а потім викликайте git merge feat/...
Після злиття непотрібну feature-гілку можна видалити командою git branch -d feat/...`}
/>
