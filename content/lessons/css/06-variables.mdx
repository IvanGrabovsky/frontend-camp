<article className="lesson-card">
      <h2>Що це таке?</h2>
      <p><strong>CSS Custom Properties</strong> (кастомні властивості, або CSS-змінні) — нативні змінні CSS. Оголошуються через `--name: value` і використовуються через `var(--name)`. На відміну від SCSS-змінних вони <em>живі</em>: змінюються через JavaScript у runtime і успадковуються в DOM.</p>
      <p>`@property` (CSS Houdini) дозволяє оголосити тип змінної — тоді браузер знає як анімувати її через `transition` та `@keyframes`.</p>
    </article>

    <section className="analogy-block">
      <div className="analogy-block__title"><span className="analogy-block__icon">🎨</span> Аналогія з палітрою художника</div>
      <p>Кастомні властивості — як іменована палітра: замість щоразу змішувати той самий «brand-blue» ти називаєш його раз і використовуєш скрізь. Захотів змінити тон — міняєш лише в палітрі (`:root`), а не по всьому полотну.</p>
    </section>

    <section>
      <h2>Оголошення та використання</h2>
      

```css
/* :root — глобальна область видимості */
:root {
  --color-primary:  #6366f1;
  --color-text:     #1e1e2e;
  --color-bg:       #f8f9fc;
  --spacing-sm:     8px;
  --spacing-md:     16px;
  --spacing-lg:     32px;
  --border-radius:  8px;
  --shadow:         0 2px 12px rgba(0,0,0,.08);
}

/* Використання */
.btn  { background: var(--color-primary); border-radius: var(--border-radius); }
.card { padding: var(--spacing-md); box-shadow: var(--shadow); }

/* Fallback — якщо змінна не визначена */
color: var(--color-accent, #ff6b6b);
```



      <h2>Темна тема через кастомні властивості</h2>
      

```css
:root { --bg: #fff; --text: #111; --surface: #f5f5f5; }

/* Варіант 1: data-атрибут на html — перемикається через JS */
[data-theme="dark"] { --bg: #0f0f1a; --text: #e2e8f0; --surface: #1a1a2e; }

/* Варіант 2: системна перевага OS */
@media (prefers-color-scheme: dark) {
  :root { --bg: #0f0f1a; --text: #e2e8f0; }
}

/* Всі елементи адаптуються автоматично */
body { background: var(--bg); color: var(--text); }
```



      <h2>Динамічне оновлення через JavaScript</h2>
      

```css
// Читання
getComputedStyle(document.documentElement).getPropertyValue('--color-primary');

// Запис
document.documentElement.style.setProperty('--color-primary', '#ff6b6b');

// Перемикач теми
btn.addEventListener('click', () =>
  document.documentElement.dataset.theme =
    document.documentElement.dataset.theme === 'dark' ? '' : 'dark'
);
```



      <h2>`@property` — типізовані змінні для анімацій</h2>
      

```css
/* Без @property браузер не знає тип — не може інтерполювати */
@property --angle {
  syntax: '<angle>';
  inherits: false;
  initial-value: 0deg;
}
.gradient {
  background: conic-gradient(from var(--angle), #6366f1, #ec4899, #6366f1);
  animation: spin 4s linear infinite;
}
@keyframes spin { to { --angle: 360deg; } }
```



      <div className="doc-links">
        <h4>Офіційна документація</h4>
        <ul>
          <li><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties" target="_blank" rel="noopener noreferrer">CSS Custom Properties — MDN</a></li>
          <li><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/@property" target="_blank" rel="noopener noreferrer">@property — MDN</a></li>
          <li><a href="https://open-props.style/" target="_blank" rel="noopener noreferrer">Open Props — готова бібліотека CSS-змінних</a></li>
        </ul>
      </div>
    </section>

    <section className="tip-block tip-block--warn">
      <div className="tip-block__title"><span>⚠️</span> Типові помилки</div>
      <ul>
        <li><strong>Плутати CSS Variables і SCSS-змінні</strong>: SCSS-змінні — статичні, компілюються у значення. CSS-змінні — живі, доступні в runtime через JS.</li>
        <li><strong>Не вказувати fallback</strong>: якщо змінна не визначена — властивість стає `initial`. Завжди: `var(--color, #000)`.</li>
        <li><strong>Незрозумілі назви</strong>: `--c1`, `--x` — погано. Використовуй семантику: `--color-primary`, `--spacing-sm`.</li>
        <li><strong>Намагатись анімувати без `@property`</strong>: без оголошення типу браузер не може інтерполювати числові значення.</li>
      </ul>
    </section>

    <section className="practice-block">
      <div className="practice-block__title"><span>⚡</span> Практика</div>
      <p className="practice-block__intro">Відкрий <a href="https://codepen.io/pen/" target="_blank" rel="noopener">CodePen</a> або VS Code і виконай:</p>
      <ol>
        <li>Створи design token систему у `:root`: кольори, відступи, радіуси. Застосуй до кількох компонентів.</li>
        <li>Реалізуй перемикач темної теми через `data-theme` атрибут і JavaScript.</li>
        <li>Зроби компонент кнопки що повністю стилізується через CSS-змінні — без жодного хардкодованого кольору.</li>
        <li>Спробуй `@property`: анімований gradient angle з прикладу вище.</li>
      </ol>
    </section>

    <section className="homework-block">
      <div className="homework-block__title"><span>📝</span> Запитання для самоперевірки</div>
      <ol>
        <li>В чому різниця між CSS Custom Properties і SCSS-змінними?</li>
        <li>Що таке fallback у `var()` і коли він спрацьовує?</li>
        <li>Як реалізувати темну тему через кастомні властивості?</li>
        <li>Як прочитати і записати CSS-змінну через JavaScript?</li>
        <li>Навіщо потрібен `@property` і що він додає?</li>
      </ol>
    </section>

    <section className="hw-review-block">
      <div className="hw-review-block__title"><span>📋</span> Типові помилки на співбесідах</div>
      <div className="hw-review-items">
        <article className="hw-review-item">
          <h4 className="hw-review-item__task">Питання: «Що таке CSS Custom Properties?»</h4>
          <p className="hw-review-item__bad"><span className="hw-review-label">❌</span> «Це те саме що SCSS змінні» — неточно.</p>
          <p className="hw-review-item__good"><span className="hw-review-label">✅</span> CSS Custom Properties — нативні змінні, оголошуються через `--name`, використовуються через `var()`. На відміну від SCSS — живі: змінюються через JS, наслідуються в DOM, доступні у runtime.</p>
        </article>
        <article className="hw-review-item">
          <h4 className="hw-review-item__task">Питання: «Як зробити темну тему?»</h4>
          <p className="hw-review-item__bad"><span className="hw-review-label">❌</span> «Окремий CSS-файл для темного режиму».</p>
          <p className="hw-review-item__good"><span className="hw-review-label">✅</span> CSS Custom Properties + `[data-theme="dark"]`: у `:root` оголошуємо змінні, у `[data-theme="dark"]` перевизначаємо їх. JS тільки перемикає атрибут — стилі оновлюються автоматично.</p>
        </article>
      </div>
    </section>

    <section className="pro-tips-block">
      <div className="pro-tips-block__title"><span>✨</span> Цікаві фішки</div>
      <ul>
        <li>CSS-змінну можна задати на будь-якому елементі — вона буде доступна тільки всередині нього. Зручно для «тем» компонентів.</li>
        <li>`color-mix(in oklch, var(--color) 50%, white)` — нативний спосіб освітлити колір без SCSS `lighten()`.</li>
        <li><a href="https://open-props.style/" target="_blank" rel="noopener">Open Props</a> — готова бібліотека CSS-змінних (кольори, тіні, анімації). Підключається одним рядком.</li>
        <li>Chrome DevTools → Elements → computed styles показує реальні значення CSS-змінних для обраного елемента.</li>
      </ul>
    </section>
